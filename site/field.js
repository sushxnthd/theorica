(()=>{const c=document.getElementById('field'),x=c.getContext('2d');let w,h,dpr,nodes=[];function size(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(dpr,0,0,dpr,0,0);const n=Math.min(70,Math.max(28,Math.floor(w/22)));nodes=Array.from({length:n},(_,i)=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.12,vy:(Math.random()-.5)*.12,r:Math.random()*1.4+.6,p:i%9===0}))}function frame(){x.clearRect(0,0,w,h);for(const a of nodes){a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>w)a.vx*=-1;if(a.y<0||a.y>h)a.vy*=-1}x.lineWidth=.45;for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<130){x.globalAlpha=(1-d/130)*.22;x.strokeStyle='#c8ff65';x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}for(const a of nodes){x.globalAlpha=a.p?.42:.18;x.fillStyle=a.p?'#c8ff65':'#ddd';x.beginPath();x.arc(a.x,a.y,a.r,0,Math.PI*2);x.fill()}x.globalAlpha=1;requestAnimationFrame(frame)}addEventListener('resize',size);size();frame()})();